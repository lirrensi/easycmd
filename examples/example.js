// in any code file use prefixes like

// easycmd> ls
// cmd> ls
// bash> ls

// so it can be useful directly in comments

// any selected text can also be run by selecting and clicking the "Run" button
// ls
